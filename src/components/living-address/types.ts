export interface IAddressInfo {
  buildingNumber: string;
  buildingName: string;
  streetNumber: number;
  streetName: string;
  suburb: string;
  city: string;
  postalCode: number;
  province: string;
}
